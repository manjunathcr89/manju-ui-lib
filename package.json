{
  "name": "manju-ui-lib",
  "version": "0.0.3",
  "description": "UI library by Manjunath",
  "main": "./dist/*",
  "files": ["./dist/*"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build",
    "move-files": "cp ./package.json ./dist/package.json && cp ./LICENSE ./dist/LICENSE && cp ./README.md ./dist/README.md && mv ./dist/style.css ./dist/styles.css",
    "publish": "npm run build && npm run move-files"
  },
  "author": "Manjunath",
  "license": "ISC",
  "devDependencies": {
    "react": "^18.2.0",
    "vite": "^5.1.6"
  }
}
